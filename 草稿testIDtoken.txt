// test ID token
    fetch(_config.api.findTagsUrl, {
        method: 'GET',
        headers: {
            "Authorized: ": idToken
        }

    })
        .then(
            response => {
                console.log(response.status);
                if (response.status != 200) {
                    console.log("Sorry, ID Token Invalid");
                    altert("Please sign in agin with valid account");
                    logout();
                }

                return response.json()
            })
        .then(result => {
            document.getElementByID("wholePage").style.visibility = "visible";
            console.log('ID Token is valid!');
        }
        )

        .catch{
        (error) => {
            console.error('Error Occured!', error);
            altert("Please sign in... .. .");
            // logout();
        }
    };